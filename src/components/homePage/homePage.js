import {Header, MainPage, WorkPage} from '../../components'

function HomePage(){
    return(
        <div>
        {/* <Header /> */}
        <MainPage />
        <WorkPage />
        </div>
    );
}

export default HomePage;
